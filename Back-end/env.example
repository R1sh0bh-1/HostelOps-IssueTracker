NODE_ENV=development
PORT=5000

# Mongo (Local)
# MONGODB_URI=mongodb://127.0.0.1:27017/hostelops_issue_tracker

# Mongo (Atlas sample) - replace <username>, <password>, <cluster>, <db>
# Example:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<db>?retryWrites=true&w=majority&appName=<appName>

# JWT
JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:8080

# Dev seed admin user
SEED_ADMIN=true
ADMIN_EMAIL=admin@hostel.edu
ADMIN_PASSWORD=warden123
ADMIN_NAME=Warden Smith
ADMIN_ROLE=management

